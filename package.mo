within ;
package EnergyCell
annotation (uses(                           PowerSystems(version="1.0.0"),
      Modelica(version="4.0.0")),
  version="1",
  conversion(noneFromVersion=""));
end EnergyCell;
